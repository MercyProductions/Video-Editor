# Sample Cinematic Glow Effect

This is a local, manifest-only example effect plugin. It documents the shape expected by plugin audits without executing plugin code.

Audit:

```powershell
python render.py plugin audit
```
