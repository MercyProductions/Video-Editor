# Sample Punch Zoom Transition

This is a Phase 5 plugin manifest example. The current engine discovers it with:

```powershell
python render.py plugin list
```

Future transition hooks can load implementation files from this folder while keeping the manifest contract stable.
